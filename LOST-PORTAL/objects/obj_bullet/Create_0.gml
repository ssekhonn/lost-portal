// Optionally override the speed here if not set during creation
// speed is being set in the player's shooting code, so this line is optional.
// speed = 8;

// Set a lifetime (in steps) so the bullet won't last forever
lifetime = 60;  // 60 steps (~1 second at 60fps)
